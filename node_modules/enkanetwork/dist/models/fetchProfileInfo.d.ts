import { IPlayerInfo, IOwner } from "../types/index";
import { PlayerInfo } from "./index";
export declare class FetchProfileInfo {
    player: PlayerInfo;
    ttl: number;
    constructor(language: string, data: {
        playerInfo: IPlayerInfo;
        ttl: number;
        owner?: IOwner;
    });
}
