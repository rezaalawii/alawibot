import { PlayerInfo } from "./index";
export declare class FetchProfileAccounts {
    accounts: {
        uid_public: boolean;
        public: boolean;
        verified: boolean;
        player: PlayerInfo;
        hash: string;
        region: string;
        order: number;
    }[];
    constructor(language: string, data: Record<string, any>);
}
